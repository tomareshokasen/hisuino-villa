# hisuino-villa 開発ルール

「ひすい野ヴィラ」公式サイト。静的HTML（ビルド工程なし）。
運用者はプログラミング初学者。説明は結論から、専門用語には一言の補足を付ける。

## AIの役割分担

    ユーザー（要件）
        │
        ▼
    メイン（Fable）── 要件定義・設計・判断・最終検証
        ├─▶ web-researcher (Sonnet) … 調べもの
        ├─▶ implementer    (Sonnet) … 実装・動作確認・修正 ※将来 Codex に置換
        └─▶ mechanic       (Haiku)  … 一括置換・フォーマット統一

メインは「判断・設計・品質ゲート」に専念する。コードを書かない、調べない、手で置換しない。

## やらないこと（必ず守る）
1. メインが一括置換・フォーマット統一をしない → mechanic に委任
2. メインが調べものをしない → web-researcher に委任
3. 2回修正して直らないものを3回目に入らない → 止めて、状況・原因の仮説・選択肢をユーザーに報告

## 進め方
1. 要件を固めてから委任する。曖昧なまま投げると手戻りが増える
2. 委任は指示書形式で渡す：目的 / 対象ファイル / 完了条件 / やってはいけないこと
3. 委任結果はメインが検証（差分の読み合わせ・動作確認）してからユーザーに報告する
4. 大きな作業は計画を立て、ユーザーの合意を得てから着手する
5. 破壊的操作（削除・上書き・force push）は実行前に必ず確認する

## セキュリティ（必ず守る）
- 秘密情報（パスワード・APIキー）をコードに書かない。.env に分け .gitignore に登録する
- フォーム入力値はサーバー側でも検証する
- ユーザー入力をそのまま HTML に出さない（エスケープする）
- 問い合わせフォームにはスパム対策（reCAPTCHA等）を入れる
- リンクは常に https://。http:// を書かない
- テストデータ・コード例に実在の個人情報（喪家名・故人名・電話番号等）を使わない
- セキュリティに関わる変更は、理由と仕組みを初心者向けに説明する

## 報告スタイル
- 日本語、結論を最初に、前置きなし
- コード修正はファイル全体を再出力せず、変更箇所の差分のみ示す
- リリース前に直すべき点があればフラグを立てる

## Codex への移行メモ（計画B）
現在は implementer (Sonnet) が実装役。Codex が使えるようになったら置き換える。
準備は Claude Code on the web の「環境設定」側で行い、リポジトリには何も書かない。
1. セットアップスクリプトに `npm install -g @openai/codex` を追加
2. 環境変数 `OPENAI_API_KEY` をシークレットとして登録（コードや CLAUDE.md には絶対に書かない）
3. Codex プラグインを有効化し、実装委任を `/codex:rescue --background` に切り替える
